package juranometria.chart;

/**
 * Deep-sky object types the chart can draw. Sprint 1 needs only galaxies;
 * further types from docs/chart-conventions.md are added when their symbols
 * are implemented.
 */
public enum DsoType {
    GALAXY
}
